# 基础
numpy
PyYAML
loguru
requests
openai

# 录音与音频处理
sounddevice
websocket-client

# 唤醒词检测
# 注意：勿升级到 1.13.x —— 1.13.4 的 macOS arm64 版本存在 KWS 检测失效问题，
# 官方测试音频也检测不到任何关键词；1.12.40 经验证正常。
sherpa-onnx==1.12.40

# （可选）扩展功能
# cryptography      # 若需加密缓存或配置
# jupyter           # 调试环境
# torch             # 本地推理（如需深度学习模型）
# transformers      # 本地大模型推理支持

# 兼容树莓派5/Jetson Orin Nano，无需额外依赖
# 适配 Python 3.8 及以上
